import React from 'react';
import { css } from '@emotion/react';
import styled from '@emotion/styled';

const CheckItem = () => {
  return <CheckItemWrapper></CheckItemWrapper>;
};

export default CheckItem;

const CheckItemWrapper = styled.li``;
