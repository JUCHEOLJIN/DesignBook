import React from 'react';
import { css } from '@emotion/react';
import ButtonGroup from './ButtonGroup';
import Button from './Button';

interface DialogProps {}

const Dialog = (prosp: DialogProps) => {
  return <div></div>;
};

export default Dialog;
